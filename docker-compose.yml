services:
  garnet:
    image: ghcr.io/microsoft/garnet
    ulimits:
      memlock: -1
    ports:
      - "6379:6379"
    volumes:
      - garnetdata:/data

  confess:
    build: .
    env_file: .env
    depends_on:
      - garnet

volumes:
  garnetdata:
